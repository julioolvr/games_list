<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @game.name %>
</p>

<p>
  <strong>Platforms:</strong>
  <%= @game.platforms.pluck(:name).join(', ') %>
</p>

<%= render 'reviews/add', game: @game %>

<%= link_to 'Edit', edit_game_path(@game) %>
<%= link_to 'Back', games_path %>
